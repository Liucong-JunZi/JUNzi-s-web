# Parallel Development Communication

This file is shared across all worktrees.
Location: /Users/liucong/code/JUNziweb/.parallel-dev/communication.md

---

## Round 2: New Bug Fixes - 2026-03-27

### Backend Tasks (Priority Order)

#### B1 (Critical): Add GET /admin/posts/:id endpoint
**Status: PENDING**

Problem: Frontend edit page calls GET /admin/posts/:id but backend only has POST/PUT/DELETE, no GET.

Files to modify:
- `backend/internal/routes/routes.go` - Add GET route
- `backend/internal/controllers/post_controller.go` - Add GetPostByID method (or reuse existing)

#### B2 (Critical): Fix Resume API field binding (snake_case)
**Status: PENDING**

Problem: Frontend sends startDate/endDate (camelCase), backend expects start_date/end_date (snake_case).
Backend should accept camelCase JSON or document the contract clearly.

Files to modify:
- `backend/internal/controllers/resume_controller.go` - Update request struct tags to accept both formats

#### B3 (High): Fix Comment creation field binding
**Status: PENDING**

Problem: Frontend sends `postId`, backend expects `post_id`.

Files to modify:
- `backend/internal/controllers/comment_controller.go` - Update CreateCommentRequest struct tags

#### B4 (High): Fix Project model field names
**Status: PENDING**

Problem: Multiple field name mismatches:
- Frontend: imageUrl, demoUrl, githubUrl, sortOrder, techStack[]
- Backend: cover_image, demo_url, github_url, sort_order, tech_stack (string)

Also status enum mismatch:
- Frontend: planning/in_progress/completed/archived
- Backend: active (default)

Files to modify:
- `backend/internal/models/models.go` - Add JSON tags for camelCase
- `backend/internal/controllers/project_controller.go` - Update request/response structs

#### B5 (High): Add search and fix query params for posts list
**Status: PENDING**

Problem:
- Frontend sends `limit`, `tag`, `search`
- Backend reads `page_size`, `tag_id`, no search logic

Files to modify:
- `backend/internal/controllers/post_controller.go` - Add search, fix param names
- `backend/internal/routes/routes.go` - If needed

#### B6 (Medium): Fix Post cover_image field binding
**Status: PENDING**

Problem: Frontend sends `coverImage`, backend expects `cover_image`.

Files to modify:
- `backend/internal/controllers/post_controller.go` - Update CreatePostRequest/UpdatePostRequest struct tags

#### B7 (Medium): Fix commentsAPI.update endpoint
**Status: PENDING**

Problem:
- Frontend expects PUT /admin/comments/:id with {content}
- Backend has PUT /admin/comments/:id/status with {status}

Need to either:
- Add PUT /admin/comments/:id for content updates, OR
- Document that only status updates are supported

Files to modify:
- `backend/internal/routes/routes.go` - Add route
- `backend/internal/controllers/comment_controller.go` - Add UpdateCommentContent method

---

### Frontend Tasks (Priority Order)

#### F1 (Critical): Fix Resume form field names
**Status: PENDING**

Problem:
1. Frontend sends startDate/endDate but backend expects start_date/end_date
2. Edit mode crashes: item.startDate.split() when backend returns start_date

Files to modify:
- `frontend/src/views/admin/AdminResume.tsx` - Use startDate/endDate in submission, handle snake_case from backend

#### F2 (High): Fix Comment creation field name
**Status: PENDING**

Problem: Frontend sends postId, backend expects post_id.

Files to modify:
- `frontend/src/api/index.ts` - Change postId to post_id in create request
- `frontend/src/views/BlogPost.tsx` - Update if needed

#### F3 (High): Fix Project model field names
**Status: PENDING**

Problem: Multiple field mismatches - see backend B4 for details.

Files to modify:
- `frontend/src/types/index.ts` - Update Project type fields
- `frontend/src/views/admin/ProjectEditor.tsx` - Use coverImage, fix status enum
- `frontend/src/views/admin/AdminProjects.tsx` - Update display fields
- `frontend/src/views/PortfolioDetail.tsx` - Update field references
- `frontend/src/api/index.ts` - Fix field mapping if needed

#### F4 (High): Fix Post list query params
**Status: PENDING**

Problem: Frontend sends limit/tag/search, backend expects page_size/tag_id.

Files to modify:
- `frontend/src/api/index.ts` - Change param names
- `frontend/src/views/Blog.tsx` - Update if needed

#### F5 (Medium): Fix Post coverImage field
**Status: PENDING**

Problem: Frontend sends coverImage, backend expects cover_image.

Files to modify:
- `frontend/src/views/admin/PostEditor.tsx` - Use cover_image in submission

---

## Notes for Integration

1. **Field Name Convention**: Backend uses snake_case (Go convention), frontend uses camelCase (JS convention).
   - Solution: Backend JSON tags should output snake_case but accept both camelCase and snake_case input
   - Alternative: Frontend sends snake_case directly

2. **API Contract**: After fixes, ensure:
   - GET /api/admin/posts/:id returns post for editing
   - POST /api/comments accepts post_id (snake_case)
   - Project fields align between frontend and backend
   - Search works in blog list

---

## Previous Round (Completed)

See archive for Round 1 fixes.