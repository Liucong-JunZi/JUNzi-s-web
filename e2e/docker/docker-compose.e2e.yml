# E2E test overlay — extends main docker-compose.yml
# Usage: docker compose -f docker-compose.yml -f e2e/docker/docker-compose.e2e.yml up -d
services:
  backend:
    environment:
      - TEST_MODE=true
      - CORS_ALLOWED_ORIGINS=http://localhost
